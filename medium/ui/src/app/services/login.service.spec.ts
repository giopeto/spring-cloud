/*import {LoginService} from './login.service';

describe('LoginService', () => {
  let service;

	
  const http = {
	// mock properties here 
  }
	  
  beforeEach(() => {
	service = new LoginService(http);
  });

	
  it('should run #signIn()', async(() => {
	// const result = signIn(user);
  }));
		
  it('should run #signUp()', async(() => {
	// const result = signUp(user);
  }));
		
  it('should run #signOut()', async(() => {
	// const result = signOut();
  }));
		
  it('should run #geCurrentUser()', async(() => {
	// const result = geCurrentUser();
  }));
		
  it('should run #userIsSignedIn()', async(() => {
	// const result = userIsSignedIn(isUserIsSignedIn);
  }));
	  
});
*/