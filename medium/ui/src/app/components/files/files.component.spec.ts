/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import {FilesComponent} from './files.component';
import {Component, Directive} from '@angular/core';
import {FilesService} from '../../services/files.service';
import {FormBuilder} from '@angular/forms';

class MockFilesService extends FilesService {
}
      
(<any>window).String = jest.fn();
      
describe('FilesComponent', () => {
  let fixture;
  let component;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [
        FilesComponent
      ],
      providers: [
        {provide: FilesService, useClass: MockFilesService},
        FormBuilder,
      ]
    }).compileComponents();
    fixture = TestBed.createComponent(FilesComponent);
    component = fixture.debugElement.componentInstance;
  });

  it('should create a component', async(() => {
    expect(component).toBeTruthy();
  }));
  
    
  it('should run #ngOnInit()', async(() => {
    // const result = component.ngOnInit();
  }));
        
  it('should run #save()', async(() => {
    // const result = component.save();
  }));
        
  it('should run #removeFromLocal()', async(() => {
    // const result = component.removeFromLocal({ id, index });
  }));
        
  it('should run #remove()', async(() => {
    // const result = component.remove({ id, index });
  }));
        
  it('should run #getFilesToDomainMapping()', async(() => {
    // const result = component.getFilesToDomainMapping(domainId);
  }));
        
  it('should run #addEventListenerToFileInput()', async(() => {
    // const result = component.addEventListenerToFileInput(filesForUpload, filesService);
  }));
        
});*/