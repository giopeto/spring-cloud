/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import {GroupsComponent} from './groups.component';
import {Component, Directive} from '@angular/core';
import {GroupsService} from '../../services/groups.service';
import {Router} from '@angular/router';

class MockGroupsService extends GroupsService {
}
      
describe('GroupsComponent', () => {
  let fixture;
  let component;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [
        GroupsComponent
      ],
      providers: [
        {provide: GroupsService, useClass: MockGroupsService},
        Router,
      ]
    }).compileComponents();
    fixture = TestBed.createComponent(GroupsComponent);
    component = fixture.debugElement.componentInstance;
  });

  it('should create a component', async(() => {
    expect(component).toBeTruthy();
  }));
  
    
  it('should run #ngOnInit()', async(() => {
    // const result = component.ngOnInit();
  }));
        
  it('should run #add()', async(() => {
    // const result = component.add();
  }));
        
  it('should run #edit()', async(() => {
    // const result = component.edit(id);
  }));
        
  it('should run #remove()', async(() => {
    // const result = component.remove({ id, index });
  }));
        
});*/